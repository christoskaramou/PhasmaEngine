# Runtime 2D Physics And Shapes

2D gameplay now uses a small Box2D-backed runtime surface alongside the existing Jolt 3D physics system. The top-level `PE_PHYSICS2D` CMake option fetches Box2D and links it into `PhasmaRuntime`; it is independent from `PE_PHYSICS`, so Jolt can remain available for 3D bodies while 2D projects use native 2D collision shapes. Editor and player hosts create the `Physics2DSystem` when the option is enabled, and `RuntimePlaySession` propagates pause/stop lifecycle state to it.

Lua exposes the generic `physics2d` table. Scripts can clear the world, set gravity, pause/unpause, add descriptor-driven bodies or box/circle/capsule helpers to scene nodes, remove bodies, set velocity/angular velocity, apply forces/impulses, set or read transforms, toggle node transform sync, and poll contact/sensor/hit events. The system runs a fixed 60 Hz Box2D step and syncs simulated body X/Y plus Z rotation back into the owning scene node while preserving the node's depth and scale. Transform sync skips unchanged bodies so static or sleeping 2D bodies do not dirty renderer state every physics step.

Box2D bodies are now editor-visible scene components. `Physics2DSystem` adds a `Component_Physics2D` tag to its owning node, stores the editable body descriptor, serializes it as `physics2d` in `.pescene`, and restores it through the shared `SceneRuntimeHooks` path on normal loads and hot-reload snapshots. The Properties panel can add/remove a Physics2D Body and edit body type, shape type, dimensions, material settings, sync flags, and filter bits. Properties also exposes lightweight panels for other Lua-authorable runtime surfaces: selected Runtime UI nodes show an authored Runtime UI component editor, and animated/skinned nodes show an Animation Runtime transport/readout panel.

The shared built-in asset root also includes `Scripts/global/ath_shapes2d.lua` for AgainstTheHero-style prototypes. It exports `ath_shapes2d` helpers that create visible 2D boxes, circles, and capsules from existing scene primitives in the XY plane, optionally attach matching `physics2d` bodies, and configure an orthographic camera looking down the 2D depth axis. This helper is project gameplay glue over generic runtime primitives and `physics2d`, not a revived engine sprite component.

`Scripts/samples/ath_physics2d_sample.lua` builds a runnable orthographic 2D smoke scene from those helpers. It clears the scene, switches to raster render mode while the sample is active, creates static bounds, dynamic boxes/circles/capsules, a kinematic paddle, and a sensor gate, then registers an always-on update callback so contacts, node sync, and body recycling can be tested from the editor without Hybrid ray-tracing TLAS rebuild cost dominating the 2D smoke.

