# PhasmaEngine Wiki

This directory is the lightweight synthesis layer referenced by the root agent instructions. It is for durable summaries, testing playbooks, and pitfalls that should be easier to scan than raw session history.

Source of truth remains the code, root `INSTRUCTIONS.md`, subsystem `INSTRUCTIONS.md` files, and MemPalace history. Keep pages concise and link back to source files or MemPalace drawers when the detail is too large for the wiki.
