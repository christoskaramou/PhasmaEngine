[package]
name = "naga-bridge"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]

[dependencies]
naga = { version = "29", features = ["wgsl-in", "spv-in", "spv-out", "hlsl-out"] }

[profile.release]
lto = true
opt-level = 3
